[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime for Netlify — handles SSR, routing and image optimization.
[[plugins]]
  package = "@netlify/plugin-nextjs"
